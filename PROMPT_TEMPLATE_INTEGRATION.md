# اتصال قالب‌های رسمی به ماژول‌های پروژه

پروژه: `C:\Projects\CaseManagement` — C# / WinForms / .NET Framework 4.7.2 / SQLite / ClosedXML 0.105

پیش از هر کاری `CLAUDE.md` و `ACCOUNTING_ARCHITECTURE.md` را بخوان. قواعد آن‌ها بر این پرامپت مقدم است.

## وضعیت فعلی

فایل `Templates\FinancialForms.xlsx` قالب رسمی چاپی مؤسسه است: ۱۱ شیت کاری + یک شیت مخفی `داده` (تبدیل عدد به حروف دری). خالی است، ۲۷۷ فورمول دارد، هر شیت یک صفحهٔ A4 راست‌به‌چپ، و **۶۱ خانهٔ نام‌گذاری‌شده (Defined Name)** دارد.

الگوی اتصال از قبل پیاده شده و کار می‌کند — گزارش «صورت حساب کلی»:

- `Accounting\AccTemplateExport.cs` — قالب را باز می‌کند، آدرس هر خانه را از `RefersTo` نامش می‌خواند، با `ws.Cell(row, col)` پر می‌کند، شیت‌های اضافی و نام‌ها را حذف و یک برگهٔ مستقل ذخیره می‌کند.
- `Accounting\AccReports.cs` — متد `ExportGeneralStatementTemplate`.
- `Accounting\FrmAccounting.cs` — دکمهٔ «خروجی روی قالب رسمی» روی کارت هر گزارش، متد `RunReportTemplate`.

**این الگو را عیناً تکرار کن.** چیز تازه‌ای اختراع نکن.

## قواعد غیرقابل‌مذاکره

1. هیچ متد، کلاس، فورم یا جدول موجودی تغییر نکند. فقط **افزودن**.
2. مسیرهای خروجی و چاپ فعلی (`ExportGeneralStatementExcel`، `PrintVoucher` و بقیه) دست‌نخورده بمانند. مسیر قالب همیشه **موازی** است.
3. هیچ جدول یا ستون دیتابیس تغییر نام ندهد و حذف نشود. ستون جدید فقط با `EnsureColumn` (افزایشی، `NULL`) و **فقط با تأیید صریح من**.
4. کتابخانهٔ جدید نصب نکن. `ClosedXML` و `System.Data.SQLite` موجودند و کافی‌اند.
5. فقط از APIهایی استفاده کن که پروژه از قبل با آن‌ها کامپایل می‌شود: `ws.Cell(row, col)`، `.Value`، `wb.Worksheet(name)`، `wb.DefinedName(name).RefersTo`، `wb.DefinedNames.DeleteAll()`، `wb.SaveAs()`. از `CellBelow`، `FirstCell`، `RowCount`، `ToStringRelative` استفاده نکن — در نسخهٔ ۰٫۱۰۵ نصب‌شده تأیید نشده‌اند.
6. اگر داده‌ای که قالب می‌خواهد در دیتابیس وجود ندارد، **بپرس**. حدس نزن، جدول نساز، مقدار جعلی ننویس.
7. هر فایل قالب جدید باید در `.csproj` با `<Content ... CopyToOutputDirectory=PreserveNewest>` ثبت شود.

## نگاشت: هر قالب به کجا برود

### الف) مالی — ماژول `Accounting\`

| شیت قالب | مقصد | داده از |
|---|---|---|
| صورت حساب کلی | ✅ انجام شده | `BuildGeneralStatement` |
| تفکیک شهریه و هزینه ها | `AccReports` + کارت گزارش ۲ | `BuildDetailedStatement` |
| حقوق | `AccReports` + کارت گزارش ۴ | `AccSalary` / `BuildSalaryStatement` |
| سند پرداخت وجه | `AccReports.PrintVoucher` — دکمهٔ «چاپ روی قالب» در فورم تراکنش | `AccTransaction` + `AccParty` |
| دفتر ثبت اسناد | تب جدید در `FrmAccounting` یا گزارش نهم | همهٔ اسناد صادره |

نکته: شیت «حقوق» ستون «نوع پرداخت» دارد (معاش / حق‌الزحمه / حق‌الماموریت) ولی `AccSalary` ندارد. **قبل از پر کردن این شیت از من بپرس** که ستون افزایشی اضافه شود یا از `Note` استنتاج گردد.

نکته: شیت مخفی `داده` مبلغ را به حروف تبدیل می‌کند و به خانهٔ جمع سند پرداخت وصل است. اگر آن شیت را حذف کنی، «مبلغ به حروف» می‌شکند — پیش از حذف، مقدارش را به‌صورت متن بنویس.

### ب) پرونده و سرپرست

| شیت قالب | مقصد | داده از |
|---|---|---|
| نامه انتقالی | `FrmCase.cs` — دکمهٔ «نامهٔ انتقالی» روی پروندهٔ باز | مشخصات پرونده، سرپرست، تعداد ایتام، ولایت مبدأ/مقصد |
| وکالت موقت | `FrmApplicant.cs` (سرپرست) — دکمهٔ «ورقهٔ وکالت موقت» | سرپرست + لیست ایتام تحت پوشش |

هر دو باید شمارهٔ پرونده و کد عمومی را از همان رکورد باز بگیرند، نه از ورودی دستی. خروجی در کنار سایر اسناد پرونده (`FrmDocs`) قابل ضمیمه شدن باشد.

### ج) اداری و کارمندان

| شیت قالب | مقصد پیشنهادی |
|---|---|
| درخواست رخصتی | ماژول جدید `Admin\` — یا اگر جای بهتری در پروژه هست، پیشنهاد بده |
| فورم ماموریت | همان ماژول؛ مبلغ حق‌الماموریت باید با سطر مربوطه در `AccSalary` قابل تطبیق باشد |
| قرارداد ترانسپورت | همان ماژول |

برای این سه، پروژه هنوز جدولی ندارد. **اول ساختار پیشنهادی را نشان بده و منتظر تأیید بمان.** جدول‌ها باید با پیشوند `Adm` و از طریق یک `AdminInitializer` به سبک `AccountingInitializer` ساخته شوند و هیچ جدول `Acc*` یا جدول پرونده را لمس نکنند.

### د) تنظیمات

شیت «فهرست و راهنما» معلومات پایهٔ مرکز است. هنگام خروجی، مقادیرش از `AccSettings` و `SecurityContext` نوشته می‌شود — مثل کاری که `AccTemplateExport` الان می‌کند. شیت جداگانه‌ای در برنامه لازم ندارد.

## روش کار

فاز به فاز. در هر فاز:

1. فایل‌های مرتبط را بخوان و بگو چه چیزی موجود است و چه چیزی نیست.
2. پلن کوتاه بده: چه فایل‌هایی، چند خط، چه ریسکی.
3. **منتظر تأیید من بمان.**
4. پیاده کن.
5. گزارش بده: دقیقاً چه تغییر کرد، و چطور تست کنم.

ترتیب پیشنهادی: گزارش‌های مالی ۲ تا ۵ → سند پرداخت وجه → نامه انتقالی → وکالت موقت → ماژول اداری.

بیش از یک فاز را در یک نوبت انجام نده.

## تعریف «تمام شد»

- پروژه بدون خطا build می‌شود.
- خروجی روی قالب تولید می‌شود و در اکسل باز می‌شود بدون `#REF!` و بدون خطای فورمول.
- هر خانه‌ای که در قالب به شیت دیگری لینک بوده، پیش از حذف آن شیت با مقدار جایگزین شده باشد.
- بخش «کنترول توازن» یا «مطابقت دارد» نشان می‌دهد یا صادقانه «—» است — هرگز عدد اشتباه.
- گزارش‌ها و چاپ‌های قبلی دقیقاً مثل قبل کار می‌کنند.
