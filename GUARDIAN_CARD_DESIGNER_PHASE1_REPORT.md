# GUARDIAN CARD DESIGNER — PHASE 1 REPORT

## بهبود قابلیت‌های Card Designer — گزارش پیاده‌سازی

| | |
|---|---|
| **Report Version** | 1.0 |
| **Date** | 2026-08-24 |
| **Scope** | Card Designer فقط (`FrmCardTemplateManager` + `CardTemplateRepository` + تزریق‌های `GuardianCardRenderer`) |
| **Explicitly out of scope** | موتور رندر/پایپ‌لاین چاپ (staging، صفحه‌بندی، PDF) — دست‌نخورده |

---

## ۱) خلاصهٔ چیزی که پیاده‌سازی شد

| درخواست | وضعیت | توضیح |
|---|---|---|
| ترتیب واقعیِ فیلدها (Drag & Drop) | ✅ پیاده‌سازی شد — با محدودهٔ مصوب | فیلدهای متنی (نام/نام پدر/تذکره/کد/نوع مددجو) آزادانه در گروهِ خودشان؛ عکس فقط قبل/بعدِ فیلدها؛ نوارِ امنیتی (QR/بارکد/امضا/مهر/هولوگرام) آزادانه در گروهِ خودش |
| ذخیرهٔ ترتیب در قالب | ✅ | `CardTemplateDesign.FieldOrderCsv` / `PhotoPosition` / `SecurityBandOrderCsv` |
| اعمال ترتیب در پیش‌نمایش و چاپِ نهایی | ✅ | همان مسیرِ رندرِ واقعی (تزریق CSS `order`)، نه یک موتورِ جدا |
| کنترل فونت/سایز/وزن/رنگ/تراز/نمایش هر فیلد | ✅ | Alignment/FontWeight به کنترل‌های قبلی (رنگ/سایز/فونت/فاصلهٔ‌خط) اضافه شد؛ Visibility از قبل با چک‌لیستِ فیلدها موجود بود |
| پیش‌نمایشِ واقعی (اندازه/روی/پشت/دادهٔ واقعی/عکس/QR) | ✅ از قبل موجود بود، فقط تأیید شد | پیش‌نمایشِ زندهٔ `FrmCardTemplateManager` از ابتدا از `CardService.BuildCardData` (پروندهٔ واقعی) استفاده می‌کرد و هر دو رو را نشان می‌داد |
| هماهنگیِ قالبِ کامل/ساده | ✅ رفع شد | رنگ/فونت/واترمارک/متن‌های اختصاصی و حالا ترتیبِ فیلدها روی هر دو قالب یکسان اعمال می‌شوند |

---

## ۲) ترتیبِ واقعیِ فیلدها — چطور کار می‌کند

### تصمیمِ معماری (با تأیید کاربر)
عکس (قابِ گردِ عمودی) و QR (مربعِ کوچکِ نواری) از نظرِ بصری با ردیف‌های متنی (برچسب:مقدار) ناهمگون‌اند و در نواحیِ کاملاً جدای کارت زندگی می‌کنند. برای اینکه «پیش‌فرض = دقیقاً چیدمانِ فعلی» تضمین‌شده بماند (بدونِ ریسک به قالب‌های موجود)، ترتیب‌دهیِ واقعی به سه گروهِ مستقل محدود شد:

1. **فیلدهای متنیِ پنلِ سرپرست** — نام، نام پدر، تذکره، کد، نوع مددجو — آزادانه در میانِ خودشان قابل‌جابه‌جایی (کشیدن‌ و رهاکردن).
2. **موقعیتِ عکس** — یک سوییچِ ساده: «قبل از فیلدها» (پیش‌فرض) یا «بعد از فیلدها».
3. **نوارِ امنیتیِ پایینِ کارتِ کامل** — QR/بارکد/امضا/مهر/هولوگرام — آزادانه در میانِ خودشان (فقط قالبِ کامل؛ قالبِ ساده نوارِ امنیتی ندارد).

### پیاده‌سازیِ فنی
هر سه ظرف از قبل `display:flex` یا `display:grid` بودند (بدونِ هیچ تغییرِ CSS)؛ فقط:
- ویژگیِ `data-order-field="X"` به عناصرِ موجودِ `index.html`/`simple.html` اضافه شد (کاملاً افزایشی، بدونِ ویژگی = بدونِ اثر).
- `GuardianCardRenderer.ApplyFieldOrderOverrides` (متدِ تازه) یک `<style>` با قواعدِ `[data-order-field="X"]{order:N}` در پوشهٔ کاریِ یک‌بارمصرف تزریق می‌کند — دقیقاً همان الگویِ اثبات‌شدهٔ `ApplyDesignOverrides`.
- برای هر گروه، به *همهٔ* عضوهای آن گروه (نه فقط آن‌هایی که کاربر جابه‌جا کرده) یک `order` صریح نوشته می‌شود؛ وگرنه فیلدهای لمس‌نشده (مثلِ «ایتام») با `order` پیش‌فرضِ ۰ به ابتدای لیست می‌پریدند.

هیچ فایلِ HTML/CSSِ پایه تغییرِ ساختاری/بصری نگرفت؛ هیچ کد جاوااسکریپتِ رویدادمحورِ جدیدی برای این بخش لازم نبود چون `order` یک ویژگیِ خالصِ CSS است (بدون ریسکِ تصاعدی‌شدن، برخلافِ باگِ قبلیِ اندازهٔ فونت).

---

## ۳) کنترل‌های طراحیِ هر فیلد

`TextFieldOverride` دو پراپرتیِ تازه گرفت: `Alignment` (راست/وسط/چپ) و `FontWeight` (۴۰۰/۵۰۰/۶۰۰/۷۰۰) — دقیقاً هم‌قراردادِ پراپرتی‌های قبلی (خالی = بدونِ تغییر، پس قالب‌های ذخیره‌شدهٔ قبلی دست‌نخورده می‌مانند). اسکریپتِ تزریقی (`applyOne` در `GuardianCardRenderer`) این دو را با **انتسابِ مطلق** اعمال می‌کند (`el.style.textAlign = ...`)، نه خواندن‌و‌ضرب‌کردن — یعنی idempotent است و حتی در چاپِ جمعی (که رویداد `guardiancard:populated` بارها شلیک می‌شود) هرگز مشکلِ «تصاعدی‌شدن» را که در فازِ قبلی برایِ اندازهٔ فونت رفع شد، تکرار نمی‌کند.

تضمینِ «فقط روی فیلدِ انتخاب‌شده اثر بگذارد» از همان معماریِ قبلیِ دیکشنریِ per-field می‌آید (بدونِ تغییر).

---

## ۴) هماهنگیِ قالبِ کامل/ساده

**علتِ قبلی:** `ApplyDesignOverrides` فقط `index.html` را می‌نوشت؛ حتی وقتی قالبِ انتخاب‌شده «ساده» بود (که اصلاً `index.html` چاپ نمی‌شود).

**رفع:** یک بلوکِ تازه در همان متد، فقط وقتی `layoutVariant == "Simple"`، همان override را با سلکتورهای معادلِ `simple.html` می‌نویسد:
- متغیرهای CSS رنگ/فونت — بدون تغییر قابلِ استفاده مجدد (هر دو فایل از قبل همان custom propertyها را دارند).
- تصویرِ پس‌زمینهٔ برگهٔ اول → `.simple-page` (معادلِ `.card-front`).
- واترمارک → همان اسکریپتِ مشترک، فقط سلکتورش به `.card-trim, .simple-trim` گسترش یافت.
- Override متنِ هر فیلد → تابعِ جدید `BuildTextOverrideScriptSimple` (سلکتورِ `data-simple-value` و رویدادِ `simplecard:populated` به‌جای `data-field`/`guardiancard:populated`) — از همان هستهٔ مشترکِ `BuildTextOverrideScriptCore` که فیکسِ idempotentِ اندازهٔ فونت را هم به ارث می‌برد (بدونِ کپیِ جداگانه‌ای که ممکن است از آن عقب بیفتد).

---

## ۵) تست‌های انجام‌شده (با شواهدِ واقعی، نه فرض)

روش: کپیِ تازهٔ بستهٔ به‌روزشده با سرورِ محلی + Edge Headless (همان روشِ فازِ قبلی).

| تست | نتیجه |
|---|---|
| بیلدِ کامل (`MSBuild`) | ✅ exit code 0، بدون خطا (سه بار در طولِ کار تکرار شد) |
| تعدادِ ویژگیِ `data-order-field` در نسخهٔ دیپلوی‌شده (`bin/Debug/GuardianCard`) | ✅ ۱۷ در `index.html`، ۱۱ در `simple.html` — دقیقاً مطابقِ انتظار |
| **پیش‌فرض بدونِ تنظیم** (۱۰ کارت) | ✅ هیچ `<style>` با `order:` تزریق نشد؛ توالیِ DOM دقیقاً همان قبلی (`PublicCode>GuardianName>FatherName>NationalID>RequestType>OrphansCount` و نوارِ امنیتی دست‌نخورده) — «قالب‌های موجود تغییر نکردند» |
| **ترتیبِ سفارشی** (۱۰ کارت، اندازه‌گیریِ واقعیِ موقعیتِ رندرشده با اسکریپتِ آزمایشی) | ✅ ترتیبِ اندازه‌گیری‌شده روی هر ۱۰ کارت دقیقاً یکسان و برابرِ محاسبهٔ موردانتظار؛ (تفاوتِ راست‌به‌چپ در خروجیِ خام، همان‌طور که برای صفحهٔ RTL طبیعی است، به‌معنیِ منطقیِ درست نگاشت شد) |
| **PDF با ترتیبِ سفارشی، ۵۰ کارت** | ✅ دقیقاً ۱۰۰ صفحه (=۲×۵۰)، `MediaBox` یکنواخت — صفحه‌بندی دست‌نخورده |
| **هماهنگیِ قالبِ ساده** (رنگ + ترتیبِ فیلد + override متنِ یک فیلد، روی ۳ کارت) | ✅ رنگِ هدر مطابقِ رنگِ تنظیم‌شده، ترتیبِ ۸ ردیف دقیقاً مطابقِ محاسبه، رنگ/تراز/وزنِ فیلدِ سفارشی‌شده روی هر ۳ کارت یکسان — پیش از این فیکس، هیچ‌کدام اثر نداشت |

---

## ۶) فایل‌های تغییر‌یافته

| فایل | نوع تغییر |
|---|---|
| `GuardianCard/index.html` | افزایشی — فقط `data-order-field="X"` روی عناصرِ موجود |
| `GuardianCard/simple.html` | افزایشی — همان الگو برایِ فیلدهای متناظر |
| `GuardianCardIntegration/CardTemplateRepository.cs` | افزودنِ `FieldOrderCsv`/`PhotoPosition`/`SecurityBandOrderCsv`/`Alignment`/`FontWeight` + آرایه‌ها/کمک‌متدهایِ همتا |
| `GuardianCardIntegration/GuardianCardRenderer.cs` | متدِ تازهٔ `ApplyFieldOrderOverrides`؛ گسترشِ `ApplyDesignOverrides` برایِ قالبِ ساده؛ `BuildTextOverrideScriptSimple` + هستهٔ مشترک؛ `layoutVariant` به‌عنوانِ پارامترِ افزایشی به `StageAndPopulate`/`StageAndPopulateBatch` |
| `GuardianCardIntegration/FrmGuardianCardPreview.cs`, `FrmGuardianCardBatchPrint.cs`, `FrmCardTemplateManager.cs` | فقط پاس‌دادنِ `layoutVariant` واقعی به فراخوانی‌هایِ موجود |
| `GuardianCardIntegration/FrmCardTemplateManager.cs` | تبِ تازهٔ «ترتیبِ فیلدها» (دو لیستِ کشیدنی + سوییچِ موقعیتِ عکس)؛ دو کمبویِ تازه (تراز/وزن) در تبِ متن‌ها |

هیچ‌کدام از `CardService.cs`، `CaseCardRepository.cs`، `GuardianCardData.cs`، مسیرهای واقعیِ چاپ/PDF در `FrmGuardianCardPreview.cs`/`FrmGuardianCardBatchPrint.cs`، یا `print.css` تغییر نکردند.

---

## ۷) محدودیت‌های شناخته‌شده (Phase 1 — نه باگ)

- عکس و QR در میانِ فیلدهای متنی قابلِ‌جاسازی نیستند (فقط عکس: قبل/بعدِ گروه؛ QR: فقط در میانِ نوارِ امنیتیِ خودش) — تصمیمِ آگاهانه برای حفظِ «پیش‌فرض = دقیقاً چیدمانِ فعلی» بدونِ تغییرِ ساختاریِ بزرگ.
- فیلدهایِ قابل‌ترتیبِ قالبِ ساده زیرمجموعه‌ای کوچک‌تر از قالبِ کامل‌اند (نام/نام پدر/تذکره) — چون قالبِ ساده اصلاً کد/نوعِ مددجو/QR ندارد؛ همان الگویِ نامتقارنِ از قبل موجودِ `ToggleableFieldsSimple`.
- دستگیرهٔ قدیمیِ چک‌لیستِ روشن/خاموشِ فیلدها («📋 فیلدهای قابل‌نمایش») همچنان فقط تزئینی است — عمداً دست‌نخورده ماند؛ ترتیبِ واقعی حالا در تبِ مجزایِ «🔀 ترتیبِ فیلدها» است.

## ۸) نتیجه

هر پنج بخشِ درخواست‌شده پیاده‌سازی و با شواهدِ عینی (نه فرض) تأیید شد. موتور رندر/پایپ‌لاینِ چاپ دست‌نخورده ماند؛ رفتارِ قالب‌های موجود (بدونِ تنظیماتِ تازه) دقیقاً همان قبلی است؛ بیلد تمیز است.
