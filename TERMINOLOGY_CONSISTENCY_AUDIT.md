# حسابرسی جامع یکپارچگی اصطلاحات — سیستم مدیریت خیریه

تاریخ حسابرسی: ۱۴۰۵/۰۶/۰۱ (2026-08-23)
دامنه: کل ریپازیتوری `C:\Projects\CaseManagement` (فرم‌ها، DAL، Enterprise، Sync، AI، Templates، Languages، اسناد)
نوع کار: **فقط حسابرسی — هیچ کد یا دیتابیسی تغییر نکرده است.**

---

## خلاصهٔ مدیریتی

خبر خوب: از فاز ۱ استانداردسازی، یک منبع واحد حقیقت به نام [`Helpers/CaseDomain.cs`](Helpers/CaseDomain.cs) ساخته شده که ۶ مقدار «وضعیت خدمات»، ۷ مقدار «نوع پرونده»، ۷ «دلیل تعلیق» و ۵ «نقش عضو» را به‌صورت آرایه‌های مرجع نگه می‌دارد و حتی نگاشتِ مقادیرِ قدیمی→استاندارد (`LegacyServiceStatusMap`, `LegacyCaseTypeMap`) دارد. اکثر فرم‌ها و گزارش‌ها از همین منبع (مستقیم یا از طریق `TblLookup`) می‌خوانند.

خبر نه‌چندان خوب: **این فایل به‌تنهایی کافی نیست** چون ۳ مسیر مستقل دیگر پیدا شد که مقادیر وضعیت را دوباره، جدا از `CaseDomain`، تعریف یا نرمال‌سازی می‌کنند — و یک ناسازگاریِ برچسبِ رابط‌کاربری (سه نام برای یک فیلد) که مستقیماً کاربر نهایی می‌بیند. هیچ‌کدام از این‌ها هنوز باعث خرابی داده نشده‌اند، اما مسیر رشدِ ناهماهنگی هستند.

عدد کل یافته‌ها: **۸ یافتهٔ عملی**، به تفکیک: ۱ Critical، ۳ High، ۲ Medium، ۲ Low.

---

## ۱. حسابرسی «وضعیت خدمات» (Service Status)

### ۱.۱ منبع مرجع (خط پایه)

فایل [`Helpers/CaseDomain.cs:34-42`](Helpers/CaseDomain.cs:34) — آرایهٔ `ServiceStatuses`، دقیقاً همان ۶ مقدار درخواستی:

```
متقاضی، در حال بررسی، در انتظار تایید (بدون همزه)، فعال، قطع، قطع موقت
```

این آرایه در `TblLookup` (دستهٔ `ServiceStatus`) seed می‌شود، در `TblCase`/`TblFamily` به‌عنوان CHECK constraint اعمال می‌شود، و `LegacyServiceStatusMap` (خطوط ۱۰۹–۱۲۱) نسخه‌های قدیمی زیر را به مقدار استاندارد نگاشت می‌کند:

| مقدار قدیمی | مقدار استاندارد |
|---|---|
| `در انتظار تأیید` (همزه) | `در انتظار تایید` |
| `در انتظار`، `درانتظار` | `در انتظار تایید` |
| `در انتظار تاييد` (ي عربی) | `در انتظار تایید` |
| `انتظار تاييد`، `انتظار تایید` | `در انتظار تایید` |
| `در حالت قطع` | `قطع` |

### ۱.۲ یافته — نوع Critical: واژگانِ وضعیت مستقل و ناهماهنگ برای متقاضی

**محل:** [`FrmApplicant.cs:95`](FrmApplicant.cs:95)
```csharp
_cmbStatus = MakeCombo(new[] { "در انتظار", "در حال بررسی", "تأیید اولیه", "رد شده" });
```
همراه با:
- [`FrmApplicant.cs:532`](FrmApplicant.cs:532) — مقدار پیش‌فرض fallback: `"در انتظار"`
- [`FrmApplicant.cs:612`](FrmApplicant.cs:612) — `UPDATE TblApplicant SET Status = 'تبدیل به پرونده' ...` (مقدار پنجم، فقط از طریق SQL، هرگز در کشویی قابل انتخاب نیست)
- [`Helpers/DatabaseInitializer.cs:330`](Helpers/DatabaseInitializer.cs:330) — `Status TEXT NOT NULL DEFAULT 'در انتظار'`

**مشکل:**
- `"در انتظار"` دقیقاً همان مقدار قدیمی/منسوخی است که `CaseDomain.LegacyServiceStatusMap` برای `ServiceStatus` آن را به `"در انتظار تایید"` مهاجرت می‌دهد — اما اینجا برای جدول دیگری (`TblApplicant`) به‌عنوان مقدار زندهٔ پیش‌فرض استفاده شده.
- `"تأیید اولیه"` و `"رد شده"` به‌عنوان وضعیت متقاضی، در هیچ‌جای `CaseDomain` وجود ندارند و توسط هیچ منطقی نرمال‌سازی نمی‌شوند.
- `"تبدیل به پرونده"` مستقیماً با SQL نوشته می‌شود و در کشویی رابط‌کاربری نامرئی است.
- برخلاف کشوی مجاورش (`_cmbRequestType`، خط ۹۴) که از `Helpers.LookupHelper.GetValues("RequestType")` می‌خواند، این کشو کاملاً هاردکد است و از `TblLookup` نمی‌خواند.

**مقدار استاندارد پیشنهادی:** یا این ۴ مقدار را به یک دستهٔ جدید `TblLookup` (مثلاً `ApplicantStatus`) منتقل کنید تا مثل بقیهٔ سیستم قابل مدیریت باشد، یا (در صورت تأیید کاربر) آن را با زیرمجموعهٔ `PreServiceStatuses` از `CaseDomain` (متقاضی / در حال بررسی / در انتظار تایید) یکی کنید.

**ریسک تغییر:** بالا — تغییر مستقیم مقدار پیش‌فرض ستون `Status` در `TblApplicant` روی داده‌های موجود اثر می‌گذارد و باید با مهاجرت داده همراه باشد.

**نیاز به مهاجرت دیتابیس:** بله — ردیف‌های موجود `TblApplicant.Status` باید بررسی و به مقادیر جدید نگاشت شوند.

**نیاز به تغییر کد:** `FrmApplicant.cs` (کشو + fallback + خط ۶۱۲)، احتمالاً `DatabaseInitializer.cs:330` (پیش‌فرض ستون).

**اولویت: Critical** — این تنها موردی است که یک جدول کامل (`TblApplicant`) دارای واژگان کاملاً موازی و مدیریت‌نشده است.

### ۱.۳ یافته — نوع High: نرمال‌سازی دوگانه و دستی در FrmCase.cs

**محل:** [`FrmCase.cs:544-551`](FrmCase.cs:544)
```sql
UPDATE TblCase SET ServiceStatus =
    CASE
        WHEN ServiceStatus = 'در حالت قطع' THEN 'قطع'
        WHEN ServiceStatus IN ('درانتظار', 'در انتظار', 'در انتظار تأیید') THEN 'در انتظار تایید'
        ELSE ServiceStatus
    END
WHERE ServiceStatus IN ('در حالت قطع', 'درانتظار', 'در انتظار', 'در انتظار تأیید')
```
این دقیقاً همان نگاشتی است که `CaseDomain.LegacyServiceStatusMap` انجام می‌دهد، اما به‌صورت مستقل و هاردکد در یک کوئری دیگر تکرار شده است.

**مشکل:** اگر در آینده مقدار قدیمی جدیدی به `LegacyServiceStatusMap` اضافه شود (مثلاً یک املای دیگر)، این کوئری در `FrmCase.cs` به‌طور خاموش از آن عقب می‌ماند چون از همان آرایه تغذیه نمی‌شود.

**مقدار استاندارد:** همان — از `CaseDomain.LegacyServiceStatusMap` بسازید (مثلاً با `SqlValueList`/یک متد کمکی که `CASE WHEN` را دینامیک تولید کند) به‌جای رشتهٔ هاردکد.

**ریسک تغییر:** پایین — تغییر فقط منبع تغذیهٔ کوئری است، نه رفتار آن (اگر درست پیاده شود، خروجی یکسان می‌ماند).

**نیاز به مهاجرت دیتابیس:** خیر.

**نیاز به تغییر کد:** `FrmCase.cs:544-551` — جایگزینی رشتهٔ هاردکد با فراخوانی به `CaseDomain`.

**اولویت: High**

### ۱.۴ یافته — نوع Medium (مستندسازی‌شده و عمدی، اما شکننده): همزه/بدون‌همزه در سه دامنهٔ مختلف

**محل‌ها:**
- [`Enterprise/EnterpriseInitializer.cs:177`](Enterprise/EnterpriseInitializer.cs:177) — `"در انتظار تأیید"` (با همزه) برای وضعیت گردش‌کار Enterprise
- [`Sync/FrmServerConnection.cs:431,461`](Sync/FrmServerConnection.cs:431) — `"در انتظار تأیید"` برای وضعیت تأیید جفت‌سازی دستگاه در Sync
- [`Helpers/LangData.cs:609-613`](Helpers/LangData.cs:609) — کامنت صریح: «هر دو املا نگه داشته می‌شوند: «تأیید» برای وضعیتِ گردش‌کارِ Enterprise و تأییدِ دستگاه در سینک، «تایید» برای وضعیت خدماتِ پرونده (استانداردِ فاز ۱). حذفِ یکی، ترجمهٔ دیگری را می‌شکند.»

**مشکل:** این مورد **عمدی و مستندشده** است، نه باگ — اما چون `CaseDomain.LegacyServiceStatusMap` دقیقاً همین رشتهٔ با‌همزه را به‌عنوان «مقدار منسوخ» برای `ServiceStatus` می‌شناسد، هر توسعه‌دهندهٔ آینده که این کامنت را نبیند ممکن است به‌اشتباه آن را «باگ» تصور کند و حذفش کند و ترجمه‌های Enterprise/Sync را بشکند.

**مقدار استاندارد:** بدون تغییر — فقط توصیه می‌شود کامنت مشابهِ `LangData.cs:609` در بالای `EnterpriseInitializer.cs:177` و `FrmServerConnection.cs:431` هم تکرار شود تا هر جای استفاده به‌صورت مستقل خودمستند باشد.

**ریسک تغییر:** بالا اگر کسی بدون دانستنِ این تفاوت، یکی را «تصحیح» کند.

**نیاز به مهاجرت دیتابیس:** خیر.

**نیاز به تغییر کد:** فقط افزودن کامنت هشدار (اختیاری، پیشنهادی).

**اولویت: Medium** (ریسک بالا اما اقدام لازم کم است — فقط مستندسازی محافظتی)

### ۱.۵ یافته — نوع Low: «غیرفعال» به دو معنای کاملاً متفاوت هم‌پوشانی دارد

`"فعال"`/`"غیرفعال"` هم‌زمان یعنی: (الف) یکی از ۶ مقدار رسمی `ServiceStatus` («فعال» تنها، «غیرفعال» هرگز مقدار معتبر ServiceStatus نیست)، و (ب) برچسبِ عمومیِ روشن/خاموش برای `IsActive` در حدود ۱۰ موجودیتِ نامرتبط: کاربران (`FrmUsers.cs`)، صندوق‌ها و طرف‌حساب‌ها (`Accounting/AccountingRepo.cs`)، ماژول‌ها و قوانین (`Enterprise/FrmModules.cs`, `FrmRules.cs`)، ردیف‌های Lookup (`FrmSettings.cs`) و غیره (~۹۰ رخداد در کل ریپازیتوری).

یک باگ واقعی که در این زمینه پیدا و **از قبل رفع شده** بود: `AI_ASSISTANT_PHASE1_FINAL_VALIDATION.md:92-96` مستند می‌کند که `PersianNluEngine.ExtractServiceStatus` عبارت‌های «قطع/متوقف» را به‌اشتباه به `"غیرفعال"` (مقدار نامعتبر) نگاشت می‌کرد. اما کد فعلی در [`AI/PersianNluEngine.cs:203-214`](AI/PersianNluEngine.cs:203) و [`AI/CaseSearchCore.cs:282-303`](AI/CaseSearchCore.cs:282) نشان می‌دهد این مورد اصلاح شده و اکنون درست به `CaseDomain.TerminatedStatuses` نگاشت می‌شود.

**مشکل:** خودِ فایل `AI_ASSISTANT_PHASE1_FINAL_VALIDATION.md` هنوز این باگ را «رفع‌نشده» گزارش می‌دهد — سندِ کهنه، نه باگِ کد.

**مقدار استاندارد:** ندارد — این صرفاً یک هشدار برای حسابرس آینده است که «غیرفعال» را به‌طور کور جست‌وجو نکند چون اکثر نتایج مربوط به `ServiceStatus` نیستند.

**ریسک تغییر:** ندارد (فقط سند باید به‌روزرسانی شود).

**نیاز به مهاجرت دیتابیس:** خیر.

**نیاز به تغییر کد:** خیر — فقط اصلاح متن `AI_ASSISTANT_PHASE1_FINAL_VALIDATION.md:92-96` برای انعکاس وضعیت واقعی.

**اولویت: Low**

### ۱.۶ برچسب‌های نمایشیِ مشتق‌شده (نه مقدار ذخیره‌شده) — فقط برای اطلاع

- `FrmDashboard.cs:703` از `"قطع شده‌ها"` (جمع، با نیم‌فاصله) در کارت داشبورد استفاده می‌کند، در حالی که `FrmDashboard.cs:2642` در نمودار روند از `"قطع شده"` (مفرد) استفاده می‌کند، و مقدار ذخیره‌شدهٔ واقعی فقط `"قطع"` است. این سه شکل نمایشی از یک مقدار، به‌نظر عمدی و برای خوانایی رابط‌کاربری است، نه باگ — اما اگر کاربر «یکسان‌سازی کامل برچسب‌های نمایشی» را هم بخواهد باید جداگانه تصمیم‌گیری شود. **اولویت: Low، صرفاً برای آگاهی.**

---

## ۲. حسابرسی «نوع پرونده / نوع درخواست» (Case Type)

### ۲.۱ منبع مرجع

[`Helpers/CaseDomain.cs:45-54`](Helpers/CaseDomain.cs:45) — آرایهٔ `CaseTypes` (۷ مقدار، ستون `TblCase.RequestType`):

```
ایتام، بدسرپرست، معلول، مهاجر، کهن‌سال، بی‌سرپرست، سایر
```

نکتهٔ مهم مستندشده در همان فایل (خطوط ۲۰–۲۲): مقدار `"یتیم"` **عمداً** برای سطح پرونده به `"ایتام"` تغییر کرد تا با `MemberRoles` (نقش عضو، که همچنان `"یتیم"` دارد) اشتباه گرفته نشود. `LegacyCaseTypeMap` (خطوط ۱۲۳–۱۲۸) این مهاجرت را مستند می‌کند: `"یتیم"→"ایتام"`، `"کهولت سن"→"کهن‌سال"`.

**نکتهٔ درخواست کاربر:** استاندارد درخواستی در این تسک «سالمند/سالمندان» بود، اما در کل کد **هیچ رخدادی از این کلمه نیست** — سیستم به‌جای آن از `"کهن‌سال"` استفاده می‌کند (با نگاشت قدیمی از `"کهولت سن"`). این خودش یک مغایرت بین سند درخواست تسک و کد واقعی است — لازم است کاربر تأیید کند که آیا می‌خواهد استاندارد نهایی `"کهن‌سال"` (فعلی) باشد یا `"سالمند"` (که نیازمند تغییر کد و دیتابیس است).

### ۲.۲ یافته — نوع High: مقادیر seed مرده/نامرتبط برای RequestType

**محل:** [`Helpers/DatabaseInitializer.cs:1156-1160`](Helpers/DatabaseInitializer.cs:1156)
```csharp
new[] { "RequestType", "ولایتی",      "0" },
new[] { "RequestType", "مهاجر",       "1" },
new[] { "RequestType", "آواره داخلی", "2" },
new[] { "RequestType", "عودت‌کننده",  "3" },
```
این ۴ مقدار (طبقه‌بندیِ «ولایتی/آوارهٔ داخلی/عودت‌کننده» — یک نظام رده‌بندیِ کاملاً متفاوت، شبیه رده‌بندی پناهندگی/مهاجرت) با `CaseDomain.CaseTypes` هیچ همخوانی ندارند، به‌جز `"مهاجر"` که تصادفاً مشترک است.

**مشکل:** این کد فقط زمانی اجرا می‌شود که دستهٔ Lookup در دیتابیس خالی باشد، و بلافاصله توسط `ReconcileLookupCategory(con, CaseDomain.CatCaseType, CaseDomain.CaseTypes)` در [`DatabaseInitializer.cs:553`](Helpers/DatabaseInitializer.cs:553) که هر ردیف خارج از `CaseDomain.CaseTypes` را `DELETE` می‌کند، بازنویسی می‌شود. یعنی **در عمل بی‌خطر است**، اما کدی است که یک نگهدارندهٔ آینده را گیج می‌کند (به‌نظر می‌رسد این ۴ مقدار زنده‌اند، در حالی‌که نیستند).

**مقدار استاندارد:** حذف این بلوکِ seed مرده و جایگزینی با ارجاع مستقیم به `CaseDomain.CaseTypes` (یا حذف کامل چون `ReconcileLookupCategory` به‌تنهایی کافی است).

**ریسک تغییر:** بسیار پایین — این کد اثر عملیاتی ندارد؛ حذفش صرفاً پاک‌سازی کد مرده است.

**نیاز به مهاجرت دیتابیس:** خیر.

**نیاز به تغییر کد:** `DatabaseInitializer.cs:1156-1160` (حذف یا جایگزینی).

**اولویت: High** (نه به‌خاطر ریسک اجرا، بلکه چون به‌راحتی می‌تواند نگهدارندهٔ بعدی را گمراه کند و در حسابرسی‌های بعدی دوباره به‌عنوان «مشکل جدید» گزارش شود)

### ۲.۳ یافته — نوع Medium: اسناد Templates با فهرست ناقص (۵ به‌جای ۷ مقدار)

**محل‌ها:**
- [`Templates/assistance-receipt/docs/FIELD_MAPPING.md:22,34`](Templates/assistance-receipt/docs/FIELD_MAPPING.md:22)
- [`Templates/assistance-receipt/docs/CSHARP_MODULE_PLAN.md:18`](Templates/assistance-receipt/docs/CSHARP_MODULE_PLAN.md:18)

هر دو فقط ۵ مقدار را فهرست می‌کنند: `ایتام، بدسرپرست، بی‌سرپرست، کهن‌سال، معلول` — و **`مهاجر` و `سایر` را کاملاً جا انداخته‌اند**.

**مشکل:** این اسناد راهنمای پیاده‌سازیِ ماژول رسید کمک‌رسانی هستند. اگر توسعه‌دهنده‌ای صرفاً از این سند پیروی کند، امکانِ انتخاب پروندهٔ نوع «مهاجر» یا «سایر» را در آن ماژول در نظر نخواهد گرفت.

**مقدار استاندارد:** به‌روزرسانی هر دو سند برای شامل‌شدنِ هر ۷ مقدار `CaseDomain.CaseTypes`.

**ریسک تغییر:** ندارد (فقط سند).

**نیاز به مهاجرت دیتابیس:** خیر.

**نیاز به تغییر کد:** خیر — فقط ویرایش Markdown.

**اولویت: Medium**

### ۲.۴ هم‌پوشانی واژگانی «معلول» بین دو دستهٔ کاملاً متفاوت — فقط برای آگاهی

- `"معلول"` به‌عنوان یکی از ۷ مقدار `CaseTypes` (نوع پروندهٔ اصلی).
- `"معلول"` هم‌زمان یکی از ۳ مقدار دستهٔ کاملاً جداگانهٔ `PhysicalStatus` است (`سالم/معلول/مریض`، [`DatabaseInitializer.cs:574`](Helpers/DatabaseInitializer.cs:574)، `FrmFamily.Designer.cs:117`، `FrmAdvancedSearch.cs:497`).
- علاوه بر این، فیلدهای سطح عضو `DisabilityType`/`DisabilityDegree`/`HasDisability` (خانوادهٔ «معلولیت») مفهوم سوم و کاملاً مجزایی هستند.

هر سه به‌درستی از نظر داده جدا هستند و باگی گزارش نشد، اما چون کلمهٔ ظاهری یکسان است، جست‌وجوی سادهٔ «معلول» در حسابرسی‌های بعدی می‌تواند این سه را قاطی کند. **اولویت: Low، صرفاً یادداشت راهنما برای حسابرس‌های بعدی.**

---

## ۳. حسابرسی اصطلاحات رابط‌کاربری (UI Terminology)

### ۳.۱ یافته — نوع High: سه برچسب متفاوت برای یک فیلد (`RequestType`)

| برچسب | محل استفاده |
|---|---|
| `"نوع درخواست"` | [`FrmCase.Designer.cs:288`](FrmCase.Designer.cs:288)، [`FrmApplicant.cs:108`](FrmApplicant.cs:108)، [`FrmAdvancedSearch.cs:184`](FrmAdvancedSearch.cs:184)، [`RptFullCase.rdlc:341`](RptFullCase.rdlc:341) |
| `"نوع پرونده"` | [`FrmCase.Designer.cs:394`](FrmCase.Designer.cs:394) (تبِ خلاصه)، [`FrmAssignMemberRole.cs:115`](FrmAssignMemberRole.cs:115) |
| `"نوع درخواستی"` | [`FrmFinance.cs:201,475`](FrmFinance.cs:201)، [`Templates/assistance-receipt/receipt.js:77`](Templates/assistance-receipt/receipt.js:77) |

**مشکل:** یک ستون واحد دیتابیس (`TblCase.RequestType`)، در سه فرم/گزارش مختلف، سه برچسب متفاوت به کاربر نشان می‌دهد. این تنها ناسازگاریِ **مستقیماً قابل‌مشاهده برای کاربر نهایی** در کل حسابرسی است.

**مقدار استاندارد پیشنهادی:** یکی از این سه (پیشنهاد: `"نوع درخواست"` چون پرتکرارترین و منطبق با نام تاریخی ستون است) را به‌عنوان استاندارد انتخاب و در سه محل دیگر جایگزین کنید.

**ریسک تغییر:** پایین — فقط تغییر برچسب متنی (`Text`/`Label`)، بدون اثر روی منطق یا داده.

**نیاز به مهاجرت دیتابیس:** خیر.

**نیاز به تغییر کد:** `FrmCase.Designer.cs:394`، `FrmAssignMemberRole.cs:115`، `FrmFinance.cs:201,475`، `Templates/assistance-receipt/receipt.js:77`.

**اولویت: High**

### ۳.۲ نمونهٔ مثبت — بدون یافته

- `"وضعیت خدمات"` به‌طور یکنواخت در `ReportDefinitions.cs`، `LangData.cs`، `FrmSettings.cs`، `RptFullCase.rdlc`، `FrmGuardianCardBatchPrint.cs` استفاده می‌شود — هیچ برچسب رقیبی برایش پیدا نشد.
- سلسله‌مراتب `متقاضی` (TblApplicant) → `پرونده` (TblCase) → `خانواده`/`عضو خانواده` (TblFamily) به‌طور یکدست در عنوان فرم‌ها و مجوزها رعایت شده؛ یافته‌ای گزارش نشد.

---

## ۴. حسابرسی دادهٔ دیتابیس (Database Data Audit)

| بررسی | نتیجه |
|---|---|
| مقادیر ذخیره‌شدهٔ فعلی | چون هیچ کوئری روی دیتابیسِ در حال اجرا زده نشد (طبق دستور «فقط حسابرسی، بدون تغییر»)، **بررسی مقادیر واقعیِ موجود در ردیف‌های `TblCase`/`TblFamily`/`TblApplicant` هنوز انجام نشده** و باید در فاز بعد (با اجازهٔ کاربر) با یک کوئری `SELECT DISTINCT ServiceStatus, RequestType FROM ...` انجام شود تا مشخص شود آیا مقادیر غیرمرجع در دادهٔ زندهٔ فعلی باقی مانده‌اند. |
| مقادیر seed/migration قدیمی | `CaseDomain.LegacyServiceStatusMap` و `LegacyCaseTypeMap` این مسیر را پوشش می‌دهند و ظاهراً به‌طور خودکار در `StandardizeCaseDomains` اجرا می‌شوند (نام تابع از کامنت `DatabaseInitializer.cs:1149` استنباط شد). |
| دادهٔ پیش‌فرض/seed مرده | بخش ۲.۲ (مقادیر `ولایتی/آواره داخلی/عودت‌کننده`) — بی‌خطر عملیاتی، اما گمراه‌کننده در کد منبع. |
| نیاز به مهاجرت | فقط بخش ۱.۲ (`TblApplicant.Status`) واقعاً نیازمند تصمیم و مهاجرتِ دادهٔ زنده است؛ بقیه یا قبلاً مهاجرت‌شده‌اند یا نیازی به مهاجرت ندارند (فقط تغییر کد/سند). |

**اقدام پیشنهادی برای فاز بعد (نیازمند تأیید کاربر):** اجرای یک کوئری فقط-خواندنی برای شمارش مقادیر فعلی هر ستون (`ServiceStatus`, `RequestType`, `TblApplicant.Status`) تا مشخص شود چند ردیف داده‌ی واقعی از مقادیر غیراستاندارد استفاده می‌کنند، پیش از هرگونه تغییر.

---

## ۵. حسابرسی یکپارچگی گزارش‌ها (Reporting Consistency)

| منبع خروجی | یافته |
|---|---|
| گزارش RDLC (`RptFullCase.rdlc`) | از `"نوع درخواست"` استفاده می‌کند (بخش ۳.۱) — با تب خلاصهٔ `FrmCase` (`"نوع پرونده"`) ناسازگار است. |
| Excel Export (`ExcelReportExporter.cs`) | در جست‌وجوی این حسابرسی، رخداد ناسازگاری مستقلی (غیر از موارد بالا) پیدا نشد؛ از فیلدهای `DisabilityType`/`MigrationCardType` مشترک با فرم‌ها استفاده می‌کند. |
| Excel Import (`ExcelCaseImporter.cs`) | همان فیلدهای بالا را می‌خواند؛ نیاز به بررسی جداگانه دارد که آیا هنگام Import، مقادیر ورودی از طریق `CaseDomain.LegacyServiceStatusMap`/`LegacyCaseTypeMap` نرمال می‌شوند یا مستقیم درج می‌شوند (این نکته در حسابرسی جاری به‌طور کامل ردیابی نشد و پیشنهاد می‌شود در فاز بعد به‌طور مجزا بررسی شود). |
| Sync (`HtmlSyncProvider.cs`, `Sync/*`) | کامنت در `CaseDomain.cs:115-116` تأیید می‌کند که HtmlSyncProvider قبلاً به‌تنهایی گونه‌های «ي عربی» را می‌شناخت و اکنون این منطق به `LegacyServiceStatusMap` منتقل شده — یعنی این مسیر ظاهراً هماهنگ شده است. |
| Print Template (`Templates/assistance-receipt`) | بخش‌های ۲.۳ و ۳.۱ (اسناد ناقص + برچسب `"نوع درخواستی"`) مستقیماً به این مسیر مربوط‌اند. |
| Dashboard (`FrmDashboard.cs`) | برچسب‌های نمایشیِ مشتق (بخش ۱.۶) — نگرانی جزئی، نه ناسازگاری دادهٔ واقعی. |
| AI Assistant Queries (`AI/PersianNluEngine.cs`, `AI/CaseSearchCore.cs`) | باگ HIGH-01 قبلاً رفع شده (بخش ۱.۵)؛ فقط سند اعتبارسنجی کهنه است. |

---

## جدول خلاصهٔ اولویت‌ها

| # | یافته | محل اصلی | اولویت | نیاز به مهاجرت DB | نیاز به تغییر کد |
|---|---|---|---|---|---|
| 1 | واژگان مستقل وضعیت متقاضی (`TblApplicant.Status`) | `FrmApplicant.cs:95,532,612` | **Critical** | بله | بله |
| 2 | نرمال‌سازی دستیِ تکراری در FrmCase | `FrmCase.cs:544-551` | **High** | خیر | بله |
| 3 | seed مردهٔ RequestType (ولایتی/آواره داخلی/عودت‌کننده) | `DatabaseInitializer.cs:1156-1160` | **High** | خیر | بله (پاک‌سازی) |
| 4 | سه برچسب متفاوت برای فیلد نوع درخواست | `FrmCase`, `FrmAssignMemberRole`, `FrmFinance`, `receipt.js` | **High** | خیر | بله |
| 5 | اسناد Templates با فهرست ناقص نوع پرونده (۵ به‌جای ۷) | `FIELD_MAPPING.md`, `CSHARP_MODULE_PLAN.md` | **Medium** | خیر | فقط سند |
| 6 | همزه/بدون‌همزه در سه دامنهٔ مختلف (عمدی، شکننده) | `EnterpriseInitializer.cs:177`, `FrmServerConnection.cs:431` | **Medium** | خیر | فقط کامنت هشدار |
| 7 | سند کهنهٔ اعتبارسنجی AI (باگ HIGH-01 قبلاً رفع شده) | `AI_ASSISTANT_PHASE1_FINAL_VALIDATION.md:92-96` | **Low** | خیر | فقط سند |
| 8 | هم‌پوشانی واژگانیِ «فعال/غیرفعال» و «معلول» بین دسته‌های مختلف | چندین فایل (~۹۰ رخداد) | **Low** | خیر | خیر (فقط یادداشت راهنما) |

---

## سؤالات باز برای تأیید کاربر پیش از پیاده‌سازی

1. **بند ۱ (Critical):** برای `TblApplicant.Status` کدام مسیر ترجیح داده می‌شود — دستهٔ Lookup جدید مستقل، یا یکی‌سازی با زیرمجموعهٔ `PreServiceStatuses` از `CaseDomain`؟ داده‌های موجود در این ستون باید پیش از تصمیم بررسی شوند.
2. **استاندارد درخواستی «سالمند/سالمندان» در برابر مقدار فعلی «کهن‌سال»:** آیا نام واقعی باید به «سالمند» تغییر کند (نیازمند مهاجرت داده + تغییر کد + تغییر گزارش‌ها)، یا سند درخواست تسک اصلاح شود تا با «کهن‌سال» (نام فعلی و پابرجا در کد) هماهنگ شود؟
3. **بند ۴ (برچسب نوع درخواست):** کدام یک از سه عبارت («نوع درخواست» / «نوع پرونده» / «نوع درخواستی») به‌عنوان استاندارد نهایی انتخاب شود؟
4. آیا اجازه داده می‌شود یک کوئری فقط-خواندنی روی دیتابیسِ فعلی اجرا شود تا مقادیر واقعیِ موجود در `ServiceStatus`/`RequestType`/`TblApplicant.Status` شمارش شوند (بخش ۴)؟

**تا دریافت پاسخ به این سؤالات و تأیید صریح کاربر، هیچ تغییری در کد یا دیتابیس اعمال نخواهد شد.**
