# بازبینی UX / UI / Workflow — ماژول معلولین

**تاریخ:** ۲۰۲۶-۰۹-۰۴ · **نوع کار:** تحلیل و طراحی — **هیچ کدی تغییر نکرد**
**نقش‌ها:** Senior UX Architect · Business Analyst · WinForms Enterprise Designer · Accessibility Reviewer

> **رعایت محدودیت‌ها:** `PROJECT_CONTEXT.md` (۷۱۲ سطر) کامل خوانده شد. هیچ تغییری در
> دیتابیس، سینک، گزارش، خروجی یا منطق کسب‌وکار انجام نشد. هر پیشنهاد با دلیل
> عملیاتی + UX + کسب‌وکاری و اثر آن بر کاربر واقعی آمده است.

---

## وضعیت فعلی — آنچه واقعاً در کد هست

**ساختار تب‌ها (۱۲ تب برای پروندهٔ معلول):**

| # | تب | نوع | محتوا |
|---|---|---|---|
| ۱ | خلاصه پرونده | خروجی | ۶ کارت آماری + ۵ کارت محاسبه‌شده (همه read-only) |
| ۲ | مشخصات کلی سرپرست | ورودی | ۱۵ فیلد |
| ۳ | مشخصات جسمی | ورودی | `chkHeadHealthy` + نوع معلولیت + درجه معلولیت |
| ۴ | مشخصات پرونده | ورودی | ۱۴ فیلد عمومی + ۹ فیلد معلولیت + گروه‌های پنهان |
| ۵ | اعضای خانواده | ورودی | `FrmFamily` تعبیه‌شده |
| ۶ | اسناد | ورودی | `FrmDocs` تعبیه‌شده |
| ۷ | نمایندهٔ قانونی | ورودی | فقط `DISABLED` |
| ۸ | بازدید میدانی | ورودی | — |
| ۹ | خانواده | ارتباط | Link/Unlink |
| ۱۰ | منابع مالی | ورودی | — |
| ۱۱ | آسیب‌پذیری | خروجی | read-only |
| ۱۲ | تاریخچه | خروجی | read-only |

**۱۱ فیلد معلولیت در دو تب پخش شده‌اند:** ۲ فیلد (نوع، درجه) در تب ۳ با کنترل
`chkHeadHealthy`؛ ۹ فیلد دیگر در تب ۴ با پرچم `ShowDisabilitySection`.

---

# SECTION A — باید قبل از تحویل اصلاح شود

## A1 — تلهٔ «سالم است»: پروندهٔ معلول با فیلدهای معلولیت **غیرفعال** باز می‌شود 🔴

**شواهد کد:**
- `FrmCase.cs:2452` → `chkHeadHealthy.Checked = true; // پیش‌فرض «سالم»`
- `UpdateHeadPhysicalState()` → وقتی تیک خورده باشد:
  `txtDisabilityType.Enabled = false`، `txtDisabilityDegree.Enabled = false`، **و متن هر دو پاک می‌شود**
- `grep chkHeadHealthy` در `FrmCase.cs` → **هیچ ارتباطی با `RequestType` وجود ندارد**

**سناریوی واقعی کاربر:**
1. اپراتور «جدید» می‌زند → `chkHeadHealthy` تیک می‌خورد (سالم).
2. در تب ۴ نوع درخواست را «معلول» انتخاب می‌کند → ۹ فیلد معلولیت ظاهر می‌شوند.
3. اپراتور همان‌جا ۹ فیلد را پر می‌کند و ذخیره می‌کند.
4. **«نوع معلولیت» و «درجه معلولیت» خالی می‌مانند** — چون در تب ۳ هستند و غیرفعال.
5. هیچ پیام خطایی نمی‌آید. پرونده ذخیره می‌شود.

**چرا این بحرانی است:**
- `DisabilityType` و `DisabilityDegree` در `TblRequiredField` برای `DISABLED` **الزامی** هستند
  (`DatabaseInitializer.cs:1707-1709`) → پرونده هرگز به ۱۰۰٪ تکمیل نمی‌رسد.
- `DisabilityDegree` یکی از factهای موتور مساعدت است → **مبلغ پیشنهادی مساعدت اشتباه محاسبه می‌شود**.
- معیار `DISABLED` در امتیاز آسیب‌پذیری وزن ۱۵ دارد.
- در گزارش RDLC فقط همین دو فیلد از ۱۱ فیلد چاپ می‌شوند → **گزارش چاپی پروندهٔ معلول خالی درمی‌آید**.

**دلیل عملیاتی:** اپراتور کم‌تجربه باید حدس بزند که برای ثبت یک معلول، باید در تبی
*دیگر* تیکی را که می‌گوید «سالم است» بردارد. هیچ چیز او را راهنمایی نمی‌کند.
**دلیل UX:** حالت پیش‌فرض باید رایج‌ترین حالتِ آن زمینه باشد. در پروندهٔ معلول،
«سالم» پیش‌فرضِ غلط است.
**دلیل کسب‌وکاری:** دادهٔ ناقص → مبلغ مساعدت غلط → پرداخت غلط به ذی‌نفع واقعی.

**اصلاح پیشنهادی (کم‌ریسک‌ترین گزینه):** هشدار نرم هنگام ذخیره — دقیقاً همان الگوی
موجودِ «تذکرهٔ تکراری» در `FrmCase`/`FrmFamily` (پیام + تأیید صریح کاربر، نه مسدودسازی):
> «نوع درخواست معلول است ولی نوع/درجهٔ معلولیت ثبت نشده. در تب مشخصات جسمی
> تیک "سالم است" را بردارید. ذخیره ادامه یابد؟»

این کار **منطق کسب‌وکار را تغییر نمی‌دهد**، مسیر ذخیره را مسدود نمی‌کند و روی
پرونده‌های موجود اثر ندارد.

> ⚠ **گزینهٔ جایگزین را انتخاب نکنید:** «برداشتن خودکار تیک وقتی نوع = معلول» ظاهراً
> بهتر است ولی `UpdateHeadPhysicalState` **متن را پاک می‌کند**؛ هر سیم‌کشی اشتباه
> می‌تواند دادهٔ موجود را بی‌صدا حذف کند — همان ردهٔ نقصِ C1.
> **DO NOT CHANGE BEFORE RELEASE.**

---

## A2 — خطاهای اعتبارسنجی یکی‌یکی نمایش داده می‌شوند 🔴

**شواهد کد:** `FrmCase.ValidateForm()` در هر بررسی `Msg.Show(...); Focus(); return false;`
انجام می‌دهد — یعنی **در هر بار ذخیره فقط اولین خطا** گفته می‌شود.

**سناریوی واقعی:** اپراتوری با ۴ فیلد ناقص، ۴ چرخهٔ «ذخیره → خطا → اصلاح → ذخیره»
را طی می‌کند. روی فرمی با ۴۰+ فیلد و ۱۲ تب، هر چرخه یعنی جست‌وجوی دستی در تب‌ها.

**نکتهٔ مهم:** الگوی درست **از قبل در همین مخزن وجود دارد** —
`CaseActivationValidator` عمداً *همهٔ* موارد برآورده‌نشده را یکجا فهرست می‌کند
(`PROJECT_CONTEXT.md`, بخش Activation Gate). دو الگوی متضاد در یک محصول.

**دلیل عملیاتی:** زمان ثبت هر پرونده را چند برابر می‌کند.
**دلیل UX:** «کشف خطا به‌صورت سریالی» شناخته‌شده‌ترین ضدالگوی فرم‌های سازمانی است.
**دلیل کسب‌وکاری:** سرعت ورود اطلاعات مستقیماً هدف اعلام‌شدهٔ این بازبینی است.

**اصلاح پیشنهادی:** جمع‌آوری همهٔ خطاها در یک `List<string>` و نمایش یکجا — بدون
تغییر در *قواعد* اعتبارسنجی، فقط در نحوهٔ گزارش آن‌ها. ریسک پایین، قابل آزمون.

---

## A3 — هیچ نشانه‌ای برای فیلد الزامی وجود ندارد 🔴

**شواهد کد:** `Helpers/FieldBox.cs` هیچ خاصیت `Required`، ستاره، یا `ToolTip` ندارد.
در کل `FrmCase.Designer.cs` صفر مورد `ToolTip` هست و در `FrmCase.cs` فقط ۳ مورد.

**دو تعریف متفاوت از «الزامی» وجود دارد و هیچ‌کدام روی فرم دیده نمی‌شوند:**

| منبع | ماهیت | برای `DISABLED` |
|---|---|---|
| `FrmCase.ValidateForm()` | مسدودکنندهٔ ذخیره | وضعیت خدمات، نوع درخواست، دلیل تعلیق، قالب تذکره |
| `TblRequiredField` | فقط درصد تکمیل | نام سرپرست، نام پدر، تذکره، تماس، ولایت، ولسوالی، **نوع/درجه/دلیل معلولیت** |

**دلیل عملیاتی:** اپراتور درصد تکمیل ۶۷٪ را می‌بیند ولی **نمی‌داند کدام فیلدها
باقی مانده‌اند**. عددی که نه توضیح دارد نه قابل اقدام است.
**دلیل UX:** ستون `DisplayName` در `TblRequiredField` دقیقاً برای همین seed شده
(«نوع معلولیت»، «درجه معلولیت»…) ولی هیچ‌جا نمایش داده نمی‌شود — داده هست، نمایش نیست.
**دلیل کسب‌وکاری:** پرونده‌های ناقص به فعال‌سازی نمی‌رسند و ذی‌نفع خدمت نمی‌گیرد.

**اصلاح پیشنهادی (بدون تغییر منطق):** در کارت «خلاصه پرونده»، کنار «درصد تکمیل»،
فهرست `DisplayName` فیلدهای پرنشده نمایش داده شود. فقط خواندن از سرویس موجود
`CaseCompletionService` — بدون قاعدهٔ جدید، بدون تغییر اسکیما.

---

# SECTION B — بهتر است اصلاح شود، ولی مانع تحویل نیست

## B1 — «نوع درخواست» کلید اصلی است ولی در تب چهارم قرار دارد 🟡

`txtRequestType` در `gridCase` (تب ۴)، در جایگاه هفتم کارت است. اما همین فیلد است
که تعیین می‌کند کدام بخش‌ها در تب ۳ و ۴ ظاهر شوند
(`UpdateRequestTypeSectionVisibility`).

**اثر بر کاربر:** اپراتور تب ۲ و ۳ را پر می‌کند، به تب ۴ می‌رسد، نوع را انتخاب
می‌کند، و ناگهان ۹ فیلد تازه ظاهر می‌شود. ترتیب ورود اطلاعات **وارونه** است.

**پیشنهاد:** «نوع درخواست» به ابتدای کارت «مشخصات پرونده» منتقل شود.
> ⚠ جابه‌جایی بصری بدون به‌روزرسانی `TabIndex` (که در `FrmCase.cs` جداگانه و
> ترتیبی تنظیم می‌شود) باعث ناهماهنگی ترتیب بصری با ترتیب کیبورد می‌شود.
> هر دو باید با هم تغییر کنند. **قبل از تحویل انجام نشود.**

## B2 — تداخل مفهومی فیلدهای آدرس 🟡

در پروندهٔ معلول این فیلدهای مکانی هم‌زمان وجود دارند:

| فیلد | تب | نوع |
|---|---|---|
| سکونت اصلی سرپرست | ۲ | متن آزاد |
| سکونت فعلی سرپرست | ۲ | متن آزاد |
| زون / ولایت / ولسوالی | ۴ | ساختاریافته |
| آدرس لوکیشن | ۴ | متن آزاد |

**رابطهٔ «ولایت» (تب ۴) با «سکونت فعلی سرپرست» (تب ۲) هیچ‌جا تعریف نشده.** برای
پروندهٔ ایتام وضع بدتر است: «ولایت/ولسوالی/قریه اقامتگاه اصلی» هم اضافه می‌شود که
مفهوماً همان «سکونت اصلی سرپرست» است — **دو ثبتِ موازی از یک واقعیت**.

**دلیل کسب‌وکاری:** «ولایت» و «ولسوالی» در `TblRequiredField` برای هر ۶ نوع الزامی‌اند
و در جست‌وجو/گزارش استفاده می‌شوند؛ فیلدهای متن آزاد نمی‌شوند. اگر اپراتور آدرس را
در فیلد آزاد بنویسد، پرونده در گزارش‌های جغرافیایی گم می‌شود.

**پیشنهاد (بدون تغییر اسکیما):** فقط تغییر برچسب‌ها تا نقش هرکدام روشن شود —
«ولایت» → «ولایت (برای گزارش‌گیری)»، «آدرس لوکیشن» → «نشانی دقیق / نقطهٔ مکانی».
تغییر متن برچسب صفر ریسک داده‌ای دارد.

## B3 — تب «خلاصه پرونده» برای پروندهٔ جدید صفحهٔ صفرهاست 🟡

تب ۱ اولین چیزی است که اپراتور می‌بیند، و برای پروندهٔ جدید همهٔ ۱۱ کارت آماری
صفر/خالی‌اند.

**پیشنهاد کم‌ریسک:** ترتیب تب‌ها **تغییر نکند** (ریسک بالا، ۱۲ تب). به‌جای آن در
`ClearForm()` تب فعال روی «مشخصات پرونده» تنظیم شود؛ برای پروندهٔ موجود «خلاصه»
بماند. یک سطر، بدون اثر بر داده.

## B4 — نبود Tooltip روی فیلدهای تخصصی 🟡

«درجه معلولیت» مقادیر «اول/دوم/سوم» دارد. برای اپراتور تازه‌کار معلوم نیست «اول»
شدیدترین است یا خفیف‌ترین. همین ابهام مستقیماً روی مبلغ مساعدت اثر می‌گذارد
(`FactDisabilityDegree`).

**پیشنهاد:** افزودن `ToolTip` به `FieldBox` (افزایشی، بدون شکستن چیزی) و متن
راهنما برای: درجه معلولیت، وضعیت کارت معلولیت، دلیل معلولیت، سیادت، اولویت‌بندی اقتصادی.

## B5 — «مذهب» با کنترلی به نام `txtReligion` 🟡

`AddCaseField(gridHead, this.label9, "مذهب", this.txtReligion)` — ولی دستهٔ
Lookup واقعی `Madhab` («اهل تشیع/اهل تسنن») است و دستهٔ `Religion`
(اسلام/مسیحیت/…) در هیچ فرمی استفاده نمی‌شود. این ناسازگاری در
`PROJECT_CONTEXT.md` مستند است. مسئلهٔ نام‌گذاری داخلی است، نه خطای کاربر.
**اولویت پایین.**

---

# SECTION C — طراحی فعلی درست است و نباید تغییر کند

| # | مورد | چرا درست است |
|---|---|---|
| C1 | **مخفی‌سازی فیلدهای ایتام در پروندهٔ معلول** | از قبل درست کار می‌کند: `orphan: 0, guardian: 0` برای `DISABLED`. نام سرپرست کودک، نسبت سرپرست، نام مکتب، سطح تحصیلات، وضعیت تحصیل و اقامتگاه اصلی همگی مخفی‌اند. با آزمون قفل شد. **دست نزنید.** |
| C2 | **داده‌محور بودن نمایش بخش‌ها** | `TblRequestType` تنها منبع حقیقت است؛ مدیر می‌تواند بدون کامپایل مجدد تنظیم کند. الگوی درست سازمانی. |
| C3 | **تب مستقل برای نمایندهٔ قانونی** | وکیل یک *موجودیت* است نه یک فیلد؛ ۲ اسلات با عکس و اعتبارسنجی. تب مستقل درست است. جایگاه آن (بعد از اسناد، قبل از بازدید) منطقی است: ابتدا هویت و مدارک، سپس نمایندگی. |
| C4 | **read-only بودن آسیب‌پذیری و مبلغ پیشنهادی** | مقادیر محاسبه‌شده هرگز نباید قابل ویرایش دستی باشند، وگرنه ممیزی بی‌معنا می‌شود. |
| C5 | **`FrmFamily` و `FrmDocs` تعبیه‌شده** | یک مسیر نوشتن برای هر داده. ساختن UI دوم = دو منبع حقیقت. |
| C6 | **جدا بودن تب تاریخچه و آخر بودن آن** | تاریخچه خروجی است نه ورودی؛ انتهای نوار درست است. |
| C7 | **stamp خودکار تأییدکنندهٔ سند** | `VerifiedBy`/`VerifiedDate` هرگز تایپ نمی‌شوند. اصل صحیح ممیزی. |
| C8 | **«نوع برگه مهاجرت» فقط برای مهاجر** | همین حالا اصلاح و آزمون شد (Decision #42). |

---

# SECTION D — طرح پیشنهادی نهایی فرم معلولین

> **اصل حاکم بر این طرح:** تب‌ها **جابه‌جا نمی‌شوند**. با ۱۲ تب، `TabIndex` ترتیبی و
> تب نماینده که به‌صورت پویا درج/حذف می‌شود، هر بازچینشی ریسک بالایی دارد.
> بهبود از طریق **نقطهٔ ورود، برچسب، راهنما و بازخورد** به دست می‌آید — نه جابه‌جایی.

## D.1 — ساختار تب‌ها (بدون تغییر ترتیب)

| # | تب | نقش | تغییر پیشنهادی |
|---|---|---|---|
| ۱ | خلاصه پرونده | مرور | + فهرست فیلدهای ناقص (A3). برای پروندهٔ **جدید** تب فعال نباشد (B3) |
| ۲ | مشخصات کلی سرپرست | هویت | فقط برچسب‌های آدرس (B2) |
| ۳ | **مشخصات جسمی** | وضعیت جسمی | هشدار نرم A1 · Tooltip درجه معلولیت (B4) |
| ۴ | مشخصات پرونده | پرونده | «نوع درخواست» به ابتدا (B1، بعد از تحویل) |
| ۵ | اعضای خانواده | افراد | بدون تغییر |
| ۶ | اسناد | مدارک | بدون تغییر |
| ۷ | نمایندهٔ قانونی | نمایندگی | بدون تغییر |
| ۸–۱۲ | بازدید، خانواده، مالی، آسیب‌پذیری، تاریخچه | تکمیلی | بدون تغییر |

## D.2 — پرسش کلیدی: آیا همهٔ ۱۱ فیلد معلولیت باید در تب «مشخصات جسمی» باشند؟

**پاسخ کارشناسی: بله از نظر مفهومی — خیر برای این نسخه.**

**چرا از نظر مفهومی درست است:** یک تب باید یک وظیفهٔ مستقل داشته باشد. امروز
«مشخصات جسمی» فقط ۲ فیلد دارد و ۹ فیلد هم‌خانواده در تبی دیگر کنار «شماره فرم» و
«نام معرف» نشسته‌اند. اپراتور برای ثبت یک واقعیت واحد بین دو تب رفت‌وبرگشت می‌کند.

**چرا الان نه — 🔴 DO NOT CHANGE BEFORE RELEASE:**

| مانع | توضیح |
|---|---|
| **دو مدل کنترل ناسازگار** | ۲ فیلد با `chkHeadHealthy` (فعال/غیرفعال + **پاک‌کردن متن**)، ۹ فیلد با `ShowDisabilitySection` (نمایش/پنهان). در یک کارت واحد، رفتار متناقض دیده می‌شود. |
| **ریسک حذف داده** | اگر `chkHeadHealthy` بر هر ۱۱ فیلد حاکم شود، تیک‌زدن آن می‌تواند شماره کارت و تاریخ‌ها را پاک کند — همان ردهٔ نقص C1. |
| **ابهام معنایی حل‌نشده** | «سالم است» یعنی *سرپرست* سالم است. پروندهٔ معلول می‌تواند سرپرست سالم و **عضو** معلول داشته باشد. ادغام، پاسخ به پرسشی را تحمیل می‌کند که هنوز کسی نداده. |
| **`TabIndex`** | ترتیب کیبورد جداگانه تنظیم شده؛ نیازمند بازچینش کامل. |
| **پوشش آزمون** | چیدمان بصری با مجموعهٔ آزمون فعلی قابل اعتبارسنجی نیست. |

**مسیر درست بعد از تحویل (به ترتیب):**
1. تصمیم‌گیری دربارهٔ معنای `chkHeadHealthy` — **پیشنهاد: دامنهٔ آن به نوع/درجه محدود
   بماند و برچسبش به «سرپرست معلولیت ندارد» تغییر کند** تا ادعای فراتر از واقعیت نکند.
2. انتقال ۹ فراخوانی `AddCaseField` از `gridCase` به `gridPhysical`، با حفظ عضویت در
   `disabilitySectionFields` (سیم‌کشی نمایش بدون تغییر می‌ماند).
3. بازچینش `TabIndex`.
4. `chkHeadHealthy` **هرگز** بر ۹ فیلد جدید حاکم نشود.
5. اعتبارسنجی: رفت‌وبرگشت کامل هر ۱۱ فیلد + رگرسیون C1 (تاریخ خالی خالی بماند) +
   رگرسیون C2 (فعال‌سازی با وضعیت کارت «ندارد»).

## D.3 — ترتیب پیشنهادی فیلدها در تب «مشخصات جسمی» (پس از ادغام آتی)

ترتیب بر اساس **فرآیند واقعی مصاحبه** است، نه ترتیب ستون‌های دیتابیس — اپراتور
اول وضعیت را می‌فهمد، بعد سراغ مدرک می‌رود:

| ترتیب | فیلد | دلیل جایگاه |
|---|---|---|
| ۱ | ☑ سرپرست معلولیت ندارد | کلید کل بخش؛ اول تصمیم، بعد جزئیات |
| ۲ | نوع معلولیت | اولین چیزی که در مصاحبه مشخص می‌شود |
| ۳ | درجه معلولیت | بلافاصله پس از نوع؛ ورودی موتور مساعدت |
| ۴ | دلیل معلولیت | تکمیل‌کنندهٔ تصویر بالینی؛ الزامی برای تکمیل |
| ۵ | شرح معلولیت | متن آزاد، پس از فیلدهای ساختاریافته |
| ۶ | نیازهای خاص | خروجی ارزیابی، نه هویت |
| ۷ | **وضعیت کارت معلولیت** | **شروع بلوک کارت** — تعیین می‌کند ۴ فیلد بعدی معنا دارند یا نه |
| ۸ | شماره کارت معلولیت | فقط وقتی «دارد» |
| ۹ | صادرکنندهٔ کارت | فقط وقتی «دارد» |
| ۱۰ | تاریخ صدور کارت | فقط وقتی «دارد» — **باید خالی‌ماندنی بماند (نقص C1)** |
| ۱۱ | تاریخ انقضای کارت | فقط وقتی «دارد» |
| ۱۲ | یادداشت معلولیت | یادداشت آزاد همیشه آخر |

**نکتهٔ طراحی مهم — بلوک کارت:** «وضعیت کارت معلولیت» از قبل قاعدهٔ کسب‌وکاری دارد
(«دارد» ⇒ عکس کارت الزامی، در غیر این صورت اختیاری — رفع C2). همین قاعده باید
**بصری** هم بشود: وقتی «ندارد» یا «در حال اقدام» است، چهار فیلد ۸ تا ۱۱ غیرفعال
شوند. این کار مستقیماً از خطای انسانی جلوگیری می‌کند: امروز اپراتور می‌تواند برای
فردی که کارت ندارد، تاریخ صدور وارد کند — که دقیقاً منشأ نقص C1 بود.
🟡 **پس از تحویل** — چون یک قاعدهٔ نمایش تازه است.

## D.4 — خلاصهٔ تصمیم‌ها بر اساس ریسک

| اقدام | ریسک | زمان |
|---|---|---|
| هشدار نرم A1 | پایین | **قبل از تحویل** |
| تجمیع خطاهای اعتبارسنجی (A2) | پایین | **قبل از تحویل** |
| نمایش فیلدهای ناقص (A3) | پایین | **قبل از تحویل** |
| تغییر برچسب آدرس (B2) | صفر | **قبل از تحویل** |
| تب پیش‌فرض پروندهٔ جدید (B3) | پایین | **قبل از تحویل** |
| Tooltip (B4) | پایین | بعد از تحویل |
| جابه‌جایی «نوع درخواست» (B1) | متوسط — `TabIndex` | 🔴 بعد از تحویل |
| ادغام ۱۱ فیلد معلولیت (D.2) | **بالا** | 🔴 **DO NOT CHANGE BEFORE RELEASE** |
| غیرفعال‌سازی بلوک کارت (D.3) | متوسط | 🔴 بعد از تحویل |
| بازچینش تب‌ها | **بالا** | 🔴 **هرگز بدون دلیل قوی** |

---

## پاسخ مستقیم به ۱۰ پرسش ویژه

| # | پرسش | پاسخ |
|---|---|---|
| ۱ | همهٔ فیلدهای معلولیت در تب وضعیت جسمی؟ | **بله مفهوماً، خیر برای این نسخه.** D.2 |
| ۲ | فیلدهای ایتام در پروندهٔ معلول حذف شوند؟ | **از قبل مخفی‌اند.** کاری لازم نیست. C1 |
| ۳ | نوع برگه مهاجرت فقط برای مهاجر؟ | **بله — اصلاح و آزمون شد.** C8 |
| ۴ | جایگاه بخش وکیل مناسب است؟ | **بله.** تب مستقل، بعد از اسناد. C3 |
| ۵ | ساختار تب‌ها برای اپراتور مناسب است؟ | **قابل قبول ولی سنگین** (۱۲ تب). بازچینش ریسک بالا دارد؛ با نقطهٔ ورود و راهنما بهبود دهید. B3 |
| ۶ | ترتیب ورود اطلاعات طبیعی است؟ | **خیر** — «نوع درخواست» کلید اصلی است ولی در تب چهارم. B1 |
| ۷ | فیلدهای آدرس واضح‌اند؟ | **خیر** — ۴ مفهوم مکانی بدون رابطهٔ تعریف‌شده. B2 |
| ۸ | نام فیلدها برای کاربر غیرفنی قابل فهم است؟ | **عمدتاً بله**، جز «درجه معلولیت» (B4) و «مذهب» (B5) |
| ۹ | بخش عکس‌ها و اسناد کاربرپسند است؟ | **تا حد زیادی** — ولی **حذف عکس ممکن نیست** (جز نمایندگان). گزارش جداگانه |
| ۱۰ | فرم در استفادهٔ روزانه باعث اشتباه می‌شود؟ | **بله — یک مورد جدی:** تلهٔ «سالم است». A1 |

---

## جمع‌بندی

ماژول معلولین از نظر **معماری** سالم است: نمایش بخش‌ها داده‌محور است، منابع حقیقت
یکتا هستند، و ممیزی درست کار می‌کند. مشکلات در لایهٔ **تعامل** است، نه ساختار.

**یک نقص جدی وجود دارد که باید قبل از تحویل رفع شود: تلهٔ «سالم است» (A1)** — یک
پروندهٔ معلول می‌تواند بدون نوع و درجهٔ معلولیت ذخیره شود، بی‌آنکه هشداری داده شود،
و این مستقیماً روی مبلغ مساعدت و گزارش چاپی اثر می‌گذارد.

سه اصلاح دیگر (A2، A3، B2) کم‌ریسک‌اند و سرعت و دقت ورود اطلاعات را به‌طور محسوس
بالا می‌برند. بقیهٔ موارد باید تا پس از تحویل صبر کنند.
